package com.nextgenbuildpro.features.estimates

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.navigation.NavController

// Placeholder simplified EstimatesScreen while full implementation is temporarily disabled to stabilize build.
@Composable
fun EstimatesScreen(navController: NavController) {
    Text("Estimates feature temporarily unavailable (build stabilization mode)")
}